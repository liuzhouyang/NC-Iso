# NC-Iso
Official implementation of "Hierarchy-Aware Neural Subgraph Matching with Enhanced Similarity Measure"

## Running the Code

To run NC-Iso on a specific dataset, use the following command:

```
python -m run --dataset_name <dataset_name>
```
